# backend/security

Security-focused modules are placed here in production structure.

Current implementation references:
- `services/cryptoService.js` for AES-256, SHA-256, RSA signing
- `services/attackDetectionService.js` for threat detection and blocking
- `services/securityStateService.js` for emergency shutdown state
