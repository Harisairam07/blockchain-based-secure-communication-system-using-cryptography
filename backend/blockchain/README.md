# backend/blockchain

Blockchain integration modules are placed here in production structure.

Current implementation references:
- `services/blockchainService.js` for write/read on-chain records
- `controllers/blockchainController.js` for verification and monitor APIs
- Solidity contract in `blockchain/contracts/SecureCommunication.sol`
